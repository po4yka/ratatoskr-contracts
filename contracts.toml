# contracts.toml — machine-readable contract metadata.
# Governance authority for every contract in this repository.
# Shape authority is the Rust type named by each [[contract.root_type]].rust_path.
# Validated by `cargo contracts check`; see the spec §4.3.

metadata_version = 1

# ---------------------------------------------------------------------------
# Closed vocabularies. Any token outside these fails the build.
# ---------------------------------------------------------------------------
[services]
known = [
  "ratatoskr-platform",
  "ratatoskr-extractor",
  "ratatoskr-github",
  "ratatoskr-knowledge",
  "ratatoskr-vault",
  "ratatoskr-chatgpt",
  "ratatoskr-claude",
  "ratatoskr-x",
  "ratatoskr-instagram",
  "ratatoskr-threads",
  "ratatoskr-telegram",
]

# Entity kinds a fixture is allowed to carry (rule R12). The wire type stays OPEN
# (EntityKind::Other preserves anything matching the token grammar); this list is
# what every fixture is checked against, so the wire is open but typos fail.
#
# It is a deliberate SUPERSET of EntityKind::KNOWN. The first five are the kinds
# this build maps to a named variant. The last three are the kinds the
# unknown-kind fixtures exercise on purpose, and each is named by exactly one
# fixture:
#   x-post        README's `"aggregate_id": "x-post:123"` — a provider-minted,
#                 non-UUID local part (D4).
#   repository    valid/unknown-aggregate-kind.json — a kind this build has never
#                 heard of survives as EntityKind::Other.
#   social_source compat/old-consumer-new-producer/future-aggregate-kind.json — a
#                 bounded context added in m5-m7 does not stop a consumer built today.
#   backup_policy vault.backup_policy.acknowledged.v1 aggregates on the policy
#                 itself: `backup_policy:<version>` names the fact being answered.
#   notification  platform.notification.raised.v1 aggregates on the raised
#                 notification itself: `notification:<uuid>`.
#   analysis      the knowledge-analysis side of a notification's `analysis_ref`
#                 correlation; the kind stays opaque to the notification contract.
# Adding a token here is a review decision: it widens what a fixture may contain,
# never what the wire type understands.
[entity_kinds]
known = [
  "ai_archive", "ai_conversation", "ai_project", "analysis", "backup_policy",
  "blob", "document", "event", "notification", "operation", "user",
  "repository", "social_source", "x-post",
]

# ---------------------------------------------------------------------------
# Lint vocabulary (§6.5). Data, not prose, so the lint can read it.
# ---------------------------------------------------------------------------
[lint]
# AGENTS.md: "Reject fields that are added with vague names such as `status`,
# `data`, `metadata`, or `timestamp` without a precise documented contract."
banned_property_names = [
  "data", "metadata", "status", "timestamp", "info", "value", "values",
  "params", "properties", "extra", "misc", "content", "object", "item",
  "result", "body", "payload",
]
# AGENTS.md principle 6: "Money, sizes, durations, and counters have explicit units."
required_numeric_suffixes = ["_bytes", "_seconds", "_ms", "_count", "_percent", "_index", "_version"]
# AGENTS.md principle 5: "Document whether a timestamp is provider-authored,
# observed, received, or processed." Every `format: date-time` property must be
# in this vocabulary.
timestamp_property_names = ["occurred_at", "accepted_at", "status_changed_at", "terminated_at", "published_at", "captured_at", "imported_at", "produced_at", "provider_created_at", "provider_updated_at", "removed_at"]
min_waiver_justification_chars = 40

# ---------------------------------------------------------------------------
# Contracts
# ---------------------------------------------------------------------------
[[contract]]
id             = "core.event_envelope"     # ^[a-z][a-z0-9_]*\.[a-z][a-z0-9_]*$
family         = "core"                    # directory under schemas/json-schema/
major_version  = 1                         # >= 1
lifecycle      = "proposed"                # proposed|accepted|published|deprecated|removed (DATA_MODEL)
classification = "internal"                # public|internal|privileged (SECURITY.md separation rule)
owner          = "ratatoskr-platform"
producers      = ["ratatoskr-extractor", "ratatoskr-knowledge", "ratatoskr-platform", "ratatoskr-x"]
consumers      = ["ratatoskr-platform", "ratatoskr-knowledge"]
crate_name     = "ratatoskr-event-envelope"
canonical_path = "crates/event-envelope/src/envelope.rs"
fixtures_dir   = "fixtures/core/event-envelope"
summary        = "Common envelope for every asynchronous domain event."

[[contract.root_type]]
rust_path      = "ratatoskr_event_envelope::EventEnvelope"
output         = "schemas/json-schema/core/event-envelope.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:core:v1:EventEnvelope"
unknown_policy = "preserve"                # preserve|reject
privacy        = "boundary_metadata"       # boundary_metadata|user_content|identity|audit
                                           # NOTE: there is no `credential` variant. A
                                           # credential-bearing contract cannot be declared at
                                           # all; it fails at parse. SECURITY.md baseline.

# Field-level governance. REQUIRED for (a) every `format: date-time` property and
# (b) every property the lint flags. Optional elsewhere.
[[contract.field]]
pointer     = "/properties/occurred_at"
authority   = "producer_asserted"          # producer_asserted|provider_authored|observed|received|processed|derived
nullability = "required"                   # required|omitted_when_absent
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant at which the fact became true in the producing bounded context. Not the publish time and not the receive time."

# Waiver for a name the lint bans. Without a waiver the build fails.
[[contract.vague_field_waiver]]
pointer       = "/properties/payload"
justification = "Name fixed by the normative wire example in ARCHITECTURE S5.2. Constrained to a JSON object; its shape is selected by event_type, whose registered schema is the precise contract."

# ---------------------------------------------------------------------------
[[contract]]
id             = "core.blob_ref"
family         = "core"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-extractor"
producers      = ["ratatoskr-extractor", "ratatoskr-github", "ratatoskr-knowledge", "ratatoskr-vault"]
consumers      = ["ratatoskr-extractor", "ratatoskr-knowledge", "ratatoskr-vault"]
crate_name     = "ratatoskr-identifiers"
canonical_path = "crates/identifiers/src/blob.rs"
fixtures_dir   = "fixtures/core/blob-ref"
summary        = "Content-addressed reference to bytes owned by one service."

[[contract.root_type]]
rust_path      = "ratatoskr_identifiers::BlobRef"
output         = "schemas/json-schema/core/blob-ref.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:core:v1:BlobRef"
unknown_policy = "reject"
privacy        = "boundary_metadata"

# ---------------------------------------------------------------------------
[[contract]]
id             = "core.error_envelope"
family         = "core"
major_version  = 1
lifecycle      = "proposed"
classification = "public"
owner          = "ratatoskr-platform"
producers      = ["ratatoskr-platform", "ratatoskr-extractor", "ratatoskr-knowledge"]
consumers      = ["ratatoskr-platform"]
crate_name     = "ratatoskr-error-contracts"
canonical_path = "crates/error-contracts/src/envelope.rs"
fixtures_dir   = "fixtures/core/error-envelope"
summary        = "Machine-actionable error with a stable code, a user-safe message and explicit retryability."

[[contract.root_type]]
rust_path      = "ratatoskr_error_contracts::ErrorEnvelope"
output         = "schemas/json-schema/core/error-envelope.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:core:v1:ErrorEnvelope"
unknown_policy = "preserve"
privacy        = "boundary_metadata"

# ---------------------------------------------------------------------------
[[contract]]
id             = "content.document"
family         = "content"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-extractor"
producers      = ["ratatoskr-extractor"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-document-contracts"
canonical_path = "crates/document-contracts/src/document.rs"
fixtures_dir   = "fixtures/content/document"
summary        = "Canonical ordered document blocks and extraction provenance."

[[contract.root_type]]
rust_path      = "ratatoskr_document_contracts::Document"
output         = "schemas/json-schema/content/document.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:content:v1:Document"
unknown_policy = "reject"
privacy        = "user_content"

# ---------------------------------------------------------------------------
[[contract]]
id             = "platform.operation_snapshot"
family         = "platform"
major_version  = 1
lifecycle      = "proposed"
classification = "public"
owner          = "ratatoskr-platform"
producers      = ["ratatoskr-platform"]
consumers      = ["ratatoskr-platform"]
crate_name     = "ratatoskr-operation-contracts"
canonical_path = "crates/operation-contracts/src/snapshot.rs"
fixtures_dir   = "fixtures/platform/operation-snapshot"
summary        = "Point-in-time public view of a long-running operation."

[[contract.root_type]]
rust_path      = "ratatoskr_operation_contracts::OperationSnapshot"
output         = "schemas/json-schema/platform/operation-snapshot.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:platform:v1:OperationSnapshot"
unknown_policy = "preserve"
privacy        = "boundary_metadata"

[[contract.field]]
pointer     = "/properties/accepted_at"
authority   = "observed"
nullability = "required"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the accepting component durably recorded the request. Platform-observed, not client-asserted."

[[contract.field]]
pointer     = "/properties/status_changed_at"
authority   = "observed"
nullability = "required"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant `status` last changed. Equal to accepted_at for the first snapshot; never earlier."

[[contract.field]]
pointer     = "/properties/terminated_at"
authority   = "observed"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the operation first reached a terminal status. Present exactly when status is terminal."

[[contract.vague_field_waiver]]
pointer       = "/properties/status"
justification = "Name fixed by ARCHITECTURE S5.4, which enumerates the seven permitted values. Closed enum with documented terminality; the contract is precise despite the generic name."

# ---------------------------------------------------------------------------
[[contract]]
id             = "platform.operation_progressed"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-platform"
producers      = ["ratatoskr-platform"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-operation-contracts"
canonical_path = "crates/operation-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/platform.operation.progressed.v1"
summary        = "Fact: an operation reached the status carried in the payload."

[[contract.root_type]]
rust_path      = "ratatoskr_operation_contracts::OperationProgressed"
output         = "schemas/events/platform.operation.progressed.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:OperationProgressed"
unknown_policy = "preserve"
privacy        = "boundary_metadata"

# Legal only when family = "events".
[contract.event]
event_type   = "platform.operation.progressed.v1"
payload_type = "ratatoskr_operation_contracts::OperationProgressed"

# ---------------------------------------------------------------------------
[[contract]]
id             = "platform.operation_reported"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-platform"
producers      = ["ratatoskr-extractor", "ratatoskr-github", "ratatoskr-knowledge", "ratatoskr-vault", "ratatoskr-x"]
consumers      = ["ratatoskr-platform"]
crate_name     = "ratatoskr-operation-contracts"
canonical_path = "crates/operation-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/platform.operation.reported.v1"
summary        = "Producer report of operation progress facts known outside Platform."

[[contract.root_type]]
rust_path      = "ratatoskr_operation_contracts::OperationReported"
output         = "schemas/events/platform.operation.reported.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:OperationReported"
unknown_policy = "preserve"
privacy        = "boundary_metadata"

[[contract.vague_field_waiver]]
pointer       = "/properties/status"
justification = "The closed OperationStatus enum defines the exact lifecycle vocabulary a producer may report; consumers never infer values from free text."

[contract.event]
event_type   = "platform.operation.reported.v1"
payload_type = "ratatoskr_operation_contracts::OperationReported"

# ---------------------------------------------------------------------------
[[contract]]
id             = "social.social_source_snapshot"
family         = "social"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-x"
producers      = ["ratatoskr-instagram", "ratatoskr-threads", "ratatoskr-x"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-social-contracts"
canonical_path = "crates/social-contracts/src/snapshot.rs"
fixtures_dir   = "fixtures/social/social-source-snapshot"
summary        = "Normalized social source plus the facts of one capture of it."

[[contract.root_type]]
rust_path      = "ratatoskr_social_contracts::SocialSourceSnapshot"
output         = "schemas/json-schema/social/social-source-snapshot.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:social:v1:SocialSourceSnapshot"
unknown_policy = "preserve"
privacy        = "user_content"

[[contract.field]]
pointer     = "/properties/published_at"
authority   = "provider_authored"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the provider says the source was published. Present only when the provider supplied it; never inferred from capture time, and never a bookmark time."

[[contract.field]]
pointer     = "/properties/captured_at"
authority   = "observed"
nullability = "required"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the producing service captured or last refreshed this record into its library. The producer's clock, not the provider's."

# ---------------------------------------------------------------------------
[[contract]]
id             = "social.source_captured"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-x"
producers      = ["ratatoskr-instagram", "ratatoskr-threads", "ratatoskr-x"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-social-contracts"
canonical_path = "crates/social-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/social.source.captured.v1"
summary        = "Fact: a social source became part of a user's library."

[[contract.root_type]]
rust_path      = "ratatoskr_social_contracts::SocialSourceCaptured"
output         = "schemas/events/social.source.captured.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:SocialSourceCaptured"
unknown_policy = "preserve"
privacy        = "user_content"

[contract.event]
event_type   = "social.source.captured.v1"
payload_type = "ratatoskr_social_contracts::SocialSourceCaptured"

# ---------------------------------------------------------------------------
[[contract]]
id             = "social.source_updated"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-x"
producers      = ["ratatoskr-instagram", "ratatoskr-threads", "ratatoskr-x"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-social-contracts"
canonical_path = "crates/social-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/social.source.updated.v1"
summary        = "Fact: an existing social source's normalized record changed."

[[contract.root_type]]
rust_path      = "ratatoskr_social_contracts::SocialSourceUpdated"
output         = "schemas/events/social.source.updated.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:SocialSourceUpdated"
unknown_policy = "preserve"
privacy        = "user_content"

[contract.event]
event_type   = "social.source.updated.v1"
payload_type = "ratatoskr_social_contracts::SocialSourceUpdated"

# ---------------------------------------------------------------------------
[[contract]]
id             = "social.source_removed"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-x"
producers      = ["ratatoskr-instagram", "ratatoskr-threads", "ratatoskr-x"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-social-contracts"
canonical_path = "crates/social-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/social.source.removed.v1"
summary        = "Fact: a user's library stopped holding a social source."

[[contract.root_type]]
rust_path      = "ratatoskr_social_contracts::SocialSourceRemoved"
output         = "schemas/events/social.source.removed.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:SocialSourceRemoved"
unknown_policy = "preserve"
privacy        = "user_content"

[[contract.field]]
pointer     = "/properties/removed_at"
authority   = "observed"
nullability = "required"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the producing service removed the record from the user's library. Observed: the producer's clock, never the provider's."

[contract.event]
event_type   = "social.source.removed.v1"
payload_type = "ratatoskr_social_contracts::SocialSourceRemoved"

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.archive_snapshot"
family         = "ai_archive"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/snapshot.rs"
fixtures_dir   = "fixtures/ai_archive/archive-snapshot"
summary        = "The whole normalized graph of one provider export import."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiArchiveSnapshot"
output         = "schemas/json-schema/ai_archive/archive-snapshot.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:ai_archive:v1:AiArchiveSnapshot"
unknown_policy = "preserve"
privacy        = "user_content"

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.project"
family         = "ai_archive"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/graph.rs"
fixtures_dir   = "fixtures/ai_archive/project"
summary        = "One normalized AI project node with its parser stamp."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiProject"
output         = "schemas/json-schema/ai_archive/project.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:ai_archive:v1:AiProject"
unknown_policy = "preserve"
privacy        = "user_content"

[[contract.field]]
pointer     = "/properties/provider_created_at"
authority   = "provider_authored"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the provider says the project was created. Present only when the export supplied it; never inferred."

[[contract.field]]
pointer     = "/properties/provider_updated_at"
authority   = "provider_authored"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the provider says the project last changed. Present only when the export supplied it; never inferred."

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.conversation"
family         = "ai_archive"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/graph.rs"
fixtures_dir   = "fixtures/ai_archive/conversation"
summary        = "One normalized AI conversation graph with parser stamp and content digest."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiConversation"
output         = "schemas/json-schema/ai_archive/conversation.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:ai_archive:v1:AiConversation"
unknown_policy = "preserve"
privacy        = "user_content"

[[contract.field]]
pointer     = "/properties/provider_created_at"
authority   = "provider_authored"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the provider says the conversation was created. Present only when the export supplied it; never inferred from import time."

[[contract.field]]
pointer     = "/properties/provider_updated_at"
authority   = "provider_authored"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the provider says the conversation last changed. Present only when the export supplied it; never inferred."

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.message"
family         = "ai_archive"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/graph.rs"
fixtures_dir   = "fixtures/ai_archive/message"
summary        = "One normalized AI message node with content parts and parser stamp."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiMessage"
output         = "schemas/json-schema/ai_archive/message.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:ai_archive:v1:AiMessage"
unknown_policy = "preserve"
privacy        = "user_content"

[[contract.field]]
pointer     = "/properties/provider_created_at"
authority   = "provider_authored"
nullability = "omitted_when_absent"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the provider says the message was written. Present only when the export supplied it; never fabricated."

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.archive_imported"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/snapshot.rs"
fixtures_dir   = "fixtures/events/ai_archive.archive.imported.v1"
summary        = "Fact: one provider export finished importing as immutable evidence."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiArchiveImport"
output         = "schemas/events/ai_archive.archive.imported.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:AiArchiveImport"
unknown_policy = "preserve"
privacy        = "user_content"

[[contract.field]]
pointer     = "/properties/imported_at"
authority   = "observed"
nullability = "required"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the producing service finished importing the export into normalized nodes. The producer's clock, not the provider's."

[contract.event]
event_type   = "ai_archive.archive.imported.v1"
payload_type = "ratatoskr_ai_archive_contracts::AiArchiveImport"

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.conversation_added"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/ai_archive.conversation.added.v1"
summary        = "Fact: a conversation entered a user's AI-archive index."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiConversationAdded"
output         = "schemas/events/ai_archive.conversation.added.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:AiConversationAdded"
unknown_policy = "preserve"
privacy        = "user_content"

[contract.event]
event_type   = "ai_archive.conversation.added.v1"
payload_type = "ratatoskr_ai_archive_contracts::AiConversationAdded"

# ---------------------------------------------------------------------------
[[contract]]
id             = "ai_archive.conversation_updated"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-chatgpt"
producers      = ["ratatoskr-chatgpt", "ratatoskr-claude"]
consumers      = ["ratatoskr-knowledge"]
crate_name     = "ratatoskr-ai-archive-contracts"
canonical_path = "crates/ai-archive-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/ai_archive.conversation.updated.v1"
summary        = "Fact: an existing conversation's normalized record changed."

[[contract.root_type]]
rust_path      = "ratatoskr_ai_archive_contracts::AiConversationUpdated"
output         = "schemas/events/ai_archive.conversation.updated.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:AiConversationUpdated"
unknown_policy = "preserve"
privacy        = "user_content"

[contract.event]
event_type   = "ai_archive.conversation.updated.v1"
payload_type = "ratatoskr_ai_archive_contracts::AiConversationUpdated"

# ---------------------------------------------------------------------------
[[contract]]
id             = "backup.desired_backup_policy"
family         = "backup"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-github"
producers      = ["ratatoskr-github"]
consumers      = ["ratatoskr-vault"]
crate_name     = "ratatoskr-backup-contracts"
canonical_path = "crates/backup-contracts/src/policy.rs"
fixtures_dir   = "fixtures/backup/desired-backup-policy"
summary        = "Versioned statement of which repositories must be preserved and at what depth."

[[contract.root_type]]
rust_path      = "ratatoskr_backup_contracts::DesiredBackupPolicy"
output         = "schemas/json-schema/backup/desired-backup-policy.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:backup:v1:DesiredBackupPolicy"
unknown_policy = "preserve"
privacy        = "boundary_metadata"

[[contract.field]]
pointer     = "/properties/produced_at"
authority   = "producer_asserted"
nullability = "required"
unit        = "instant_rfc3339_utc_canonical"
note        = "Instant the producing service generated this policy document, on its own clock. Producer-asserted: neither a publish nor a receive instant, and unverifiable by the consumer."

# ---------------------------------------------------------------------------
[[contract]]
id             = "vault.backup_policy_acknowledged"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-vault"
producers      = ["ratatoskr-vault"]
consumers      = ["ratatoskr-github"]
crate_name     = "ratatoskr-backup-contracts"
canonical_path = "crates/backup-contracts/src/events.rs"
fixtures_dir   = "fixtures/events/vault.backup_policy.acknowledged.v1"
summary        = "Fact: Vault answered one desired-backup-policy version with accepted or rejected."

[[contract.root_type]]
rust_path      = "ratatoskr_backup_contracts::PolicyAcknowledged"
output         = "schemas/events/vault.backup_policy.acknowledged.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:PolicyAcknowledged"
unknown_policy = "preserve"
privacy        = "boundary_metadata"

[contract.event]
event_type   = "vault.backup_policy.acknowledged.v1"
payload_type = "ratatoskr_backup_contracts::PolicyAcknowledged"

# ---------------------------------------------------------------------------
[[contract]]
id             = "platform.notification_raised"
family         = "events"
major_version  = 1
lifecycle      = "proposed"
classification = "internal"
owner          = "ratatoskr-platform"
producers      = ["ratatoskr-knowledge", "ratatoskr-github", "ratatoskr-vault", "ratatoskr-x"]
consumers      = ["ratatoskr-telegram"]
crate_name     = "ratatoskr-notification-contracts"
canonical_path = "crates/notification-contracts/src/payload.rs"
fixtures_dir   = "fixtures/events/platform.notification.raised.v1"
summary        = "Fact: a producer judged that one of its users should be told something."

[[contract.root_type]]
rust_path      = "ratatoskr_notification_contracts::NotificationRaised"
output         = "schemas/events/platform.notification.raised.v1.schema.json"
schema_id      = "urn:ratatoskr:contracts:events:v1:NotificationRaised"
unknown_policy = "preserve"
privacy        = "user_content"

[contract.event]
event_type   = "platform.notification.raised.v1"
payload_type = "ratatoskr_notification_contracts::NotificationRaised"
