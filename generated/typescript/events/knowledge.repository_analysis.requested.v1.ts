/*
 * generated: DO NOT EDIT — generated by contractsc from the canonical Rust source. Change the Rust type and run `cargo contracts generate`.
 *
 * canonical_source: ratatoskr_github_contracts::RepositoryAnalysisRequested
 * contract_id: knowledge.repository_analysis_requested
 * contract_major_version: 1
 * generator: contractsc
 * generator_version: 0.1.0
 * schemars_version: 1.2.2
 * source_digest: sha256:7e45058ba69f6dd4c15111a19dffc68f4d700b20fda4ede65dbb09c26cb92ef4
 * validation_note: This schema is a LOWER BOUND on validity. Cross-field invariants and canonical-form rules are enforced by the canonical Rust type; see fixtures/invalid-expectations.toml for which layer rejects what.
 */
/**
 * Payload of `knowledge.repository_analysis.requested.v1`.
 *
 * GitHub Catalog requests admission only. Knowledge owns budget admission, work execution,
 * retries and the analysis result; a request remains pending until it receives one terminal fact.
 */
export interface RepositoryAnalysisRequested {
 /**
  * Stable GitHub numeric repository identity, never derived from `owner/name`.
  *
  * Format: uint64.
  */
 github_repository_numeric_id: number;
 /**
  * Catalog-generated idempotency key for the same repository revision and contract.
  */
 idempotency_key: ContentDigest;
 /**
  * User whose repository policy caused this request.
  */
 owner: TenantRef;
 /**
  * Catalog-owned identity of the repository to analyse.
  */
 repository_id: RepositoryId;
 /**
  * Join key minted once for this accepted request.
  */
 request_id: RepositoryAnalysisRequestId;
 /**
  * Exact analysis family requested. This first contract admits no alternative major.
  */
 requested_contract: RepositoryAnalysisContract;
 /**
  * Immutable input revision Knowledge must analyse.
  */
 source_revision: RepositoryAnalysisRevision;
 [key: string]: unknown;
}

/**
 * Deployment identity of the service that owns and resolves a blob.
 */
export type BlobOwner = string;

/**
 * Reference to content-addressed bytes owned by one service.
 *
 * This is a reference, not a storage API. The owner writes and resolves the bytes under its own
 * content-addressed directory. No host, filesystem path, signed URL, credentials, or expiry can
 * appear in this contract.
 */
export interface BlobRef {
 /**
  * Digest that names the bytes and lets a reader verify them.
  */
 digest: ContentDigest;
 /**
  * Exact byte length of the stored value.
  *
  * Format: uint64.
  */
 length_bytes: number;
 /**
  * Media type of the stored bytes.
  */
 media_type: MediaType;
 /**
  * Service that owns the bytes and their lifecycle.
  */
 owner_service: BlobOwner;
}

/**
 * A content digest with an explicit algorithm.
 */
export interface ContentDigest {
 /**
  * Hash algorithm used to produce `hex`.
  */
 algorithm: DigestAlgorithm;
 /**
  * Lowercase hexadecimal digest bytes.
  */
 hex: DigestHex;
}

/**
 * Algorithm used to calculate a content digest.
 */
export type DigestAlgorithm = "sha256";

/**
 * Lowercase hexadecimal bytes of a content digest.
 */
export type DigestHex = string;

/**
 * Internet media type of stored bytes, without parameters.
 */
export type MediaType = string;

/**
 * Safe reasons why a README is absent from the immutable input.
 */
export type ReadmeAbsenceReason = "not_found" | "not_authorized" | "not_preserved";

/**
 * README input state for repository analysis.
 */
export type ReadmeRevision = { /** * Reference to the exact README bytes. The reference carries its content digest. */ content_ref: BlobRef; state: "present"; } | { /** * Why a README is absent, without leaking provider diagnostics or private content. */ reason: ReadmeAbsenceReason; state: "absent"; };

/**
 * The only repository-analysis family available in this contract.
 */
export type RepositoryAnalysisContract = "repository_analysis";

/**
 * Identity of one repository-analysis request. Bare canonical lowercase hyphenated UUID; used to join terminal facts to the accepted request.
 *
 * Format: uuid.
 */
export type RepositoryAnalysisRequestId = string;

/**
 * Immutable metadata and README state of the repository input.
 */
export interface RepositoryAnalysisRevision {
 /**
  * Digest of the normalized repository attributes observed by GitHub Catalog.
  */
 attributes_digest: ContentDigest;
 /**
  * README state for this exact repository revision.
  */
 readme: ReadmeRevision;
}

/**
 * Identity of one repository record in the GitHub Catalog. Bare canonical lowercase hyphenated UUID; not GitHub's numeric repository identifier.
 *
 * Format: uuid.
 */
export type RepositoryId = string;

/**
 * The owner of the data a record concerns, wire form `user:<uuid>`. The kind set is closed: a consumer that cannot understand the owner kind must reject the record rather than process it.
 */
export type TenantRef = string;
