/*
 * generated: DO NOT EDIT — generated by contractsc from the canonical Rust source. Change the Rust type and run `cargo contracts generate`.
 *
 * canonical_source: ratatoskr_operational_contracts::PublicStatusDocument
 * contract_id: operational.public_status_document
 * contract_major_version: 1
 * generator: contractsc
 * generator_version: 0.1.0
 * schemars_version: 1.2.2
 * source_digest: sha256:629b68a759cb9f06b72cbc56bcdb8387ac59797e35ba852574e1883a957d91f5
 * validation_note: This schema is a LOWER BOUND on validity. Cross-field invariants and canonical-form rules are enforced by the canonical Rust type; see fixtures/invalid-expectations.toml for which layer rejects what.
 */
/**
 * Anonymous sanitized public status response.
 */
export interface PublicStatusDocument {
 /**
  * Stable component observations in contract order.
  */
 components: PublicStatusComponent[];
 /**
  * Instant this projection was produced by Platform.
  */
 generated_at: WireTimestamp;
 /**
  * Overall public availability.
  */
 state: PublicStatusState;
}

/**
 * A stable public component group that reveals no private topology.
 */
export type PublicComponentId = "api" | "storage" | "command_delivery" | "connected_services";

/**
 * Availability of one public component group.
 */
export type PublicComponentState = "operational" | "degraded" | "unavailable" | "unknown";

/**
 * Sanitized observation of one stable public component group.
 */
export interface PublicStatusComponent {
 /**
  * Stable public component identifier.
  */
 id: PublicComponentId;
 /**
  * Latest successful observation, absent when never observed.
  */
 observed_at?: WireTimestamp | null;
 /**
  * Whether the last successful observation is older than the current failed refresh.
  */
 stale: boolean;
 /**
  * Latest contracted component state.
  */
 state: PublicComponentState;
}

/**
 * Overall public availability of Ratatoskr.
 */
export type PublicStatusState = "operational" | "degraded" | "unavailable";

/**
 * An instant on the wire: RFC 3339 in UTC with the literal `Z` designator. Exactly one spelling is accepted per instant — an input that does not re-render to itself is rejected. The field holding it declares the clock authority.
 *
 * Format: date-time.
 */
export type WireTimestamp = string;
