/*
 * generated: DO NOT EDIT — generated by contractsc from the canonical Rust source. Change the Rust type and run `cargo contracts generate`.
 *
 * canonical_source: ratatoskr_channel_digest_contracts::KnowledgeChannelDigestRecapRequested
 * contract_id: knowledge.channel_digest_recap_requested
 * contract_major_version: 1
 * generator: contractsc
 * generator_version: 0.1.0
 * schemars_version: 1.2.2
 * source_digest: sha256:8bb68150e849f22ce3fd8026a5af035d73935accef789e0cbb40f8b4c44b41df
 * validation_note: This schema is a LOWER BOUND on validity. Cross-field invariants and canonical-form rules are enforced by the canonical Rust type; see fixtures/invalid-expectations.toml for which layer rejects what.
 */
/**
 * Payload of `knowledge.channel_digest_recap.requested.v1`.
 */
export interface KnowledgeChannelDigestRecapRequested {
 /**
  * Fixed structured-result contract.
  */
 analysis_contract: ChannelDigestAnalysisContract;
 /**
  * Fixed analysis family.
  */
 analysis_family: ChannelDigestAnalysisFamily;
 /**
  * Number of public channels represented by those sources.
  */
 channel_count: DigestChannelCount;
 /**
  * Digest run whose immutable source manifest is analyzed.
  */
 digest_run_id: ChannelDigestRunId;
 /**
  * SHA-256 identity of the canonical manifest bytes.
  */
 manifest_digest: ContentDigest;
 /**
  * Owner-authorized source interface reference.
  */
 manifest_ref: ChannelDigestManifestRef;
 /**
  * Platform operation owning this recap.
  */
 operation_id: OperationId;
 /**
  * Requested recap language.
  */
 output_language: OutputLanguage;
 /**
  * Authenticated internal user owner.
  */
 owner: TenantRef;
 /**
  * Number of immutable source revisions in the manifest.
  */
 source_count: DigestSourceCount;
 /**
  * Closed-open source publication window.
  */
 window: DigestWindow;
 [key: string]: unknown;
}

/**
 * Fixed result contract accepted by the first-version recap pipeline.
 */
export type ChannelDigestAnalysisContract = "channel_digest_recap.v1";

/**
 * Fixed analysis family accepted by the first-version recap pipeline.
 */
export type ChannelDigestAnalysisFamily = "channel_digest_recap";

/**
 * Owner-authorized immutable source-manifest reference; never a storage URL.
 */
export type ChannelDigestManifestRef = string;

/**
 * Canonical channel-digest UUID identity.
 *
 * Format: uuid.
 */
export type ChannelDigestRunId = string;

/**
 * A content digest with an explicit algorithm.
 */
export interface ContentDigest {
 /**
  * Hash algorithm used to produce `hex`.
  */
 algorithm: DigestAlgorithm;
 /**
  * Lowercase hexadecimal digest bytes.
  */
 hex: DigestHex;
}

/**
 * Algorithm used to calculate a content digest.
 */
export type DigestAlgorithm = "sha256";

/**
 * Positive bounded channel-digest count.
 */
export type DigestChannelCount = number;

/**
 * Lowercase hexadecimal bytes of a content digest.
 */
export type DigestHex = string;

/**
 * Positive bounded channel-digest count.
 */
export type DigestSourceCount = number;

/**
 * Closed-open UTC source window for one digest run.
 */
export interface DigestWindow {
 /**
  * Exclusive upper bound.
  */
 end_at: WireTimestamp;
 /**
  * Inclusive lower bound.
  */
 start_at: WireTimestamp;
}

/**
 * Identity of one long-running operation, stable for its whole lifecycle. Bare canonical lowercase hyphenated UUID; not namespaced.
 *
 * Format: uuid.
 */
export type OperationId = string;

/**
 * Language of the requested first-version recap projection.
 */
export type OutputLanguage = "ru" | "en";

/**
 * The owner of the data a record concerns, wire form `user:<uuid>`. The kind set is closed: a consumer that cannot understand the owner kind must reject the record rather than process it.
 */
export type TenantRef = string;

/**
 * An instant on the wire: RFC 3339 in UTC with the literal `Z` designator. Exactly one spelling is accepted per instant — an input that does not re-render to itself is rejected. The field holding it declares the clock authority.
 *
 * Format: date-time.
 */
export type WireTimestamp = string;
