/*
 * generated: DO NOT EDIT — generated by contractsc from the canonical Rust source. Change the Rust type and run `cargo contracts generate`.
 *
 * canonical_source: ratatoskr_github_contracts::RepositoryActionRequest
 * contract_id: github.repository_action_request
 * contract_major_version: 1
 * generator: contractsc
 * generator_version: 0.1.0
 * schemars_version: 1.2.2
 * source_digest: sha256:acf7a4422047040a8e8aeca093519d635b8ab89f2f227e3bab025c200f4de1e9
 * validation_note: This schema is a LOWER BOUND on validity. Cross-field invariants and canonical-form rules are enforced by the canonical Rust type; see fixtures/invalid-expectations.toml for which layer rejects what.
 */
export interface RepositoryActionRequest {
 /**
  * Acting connected account when required by the mode.
  */
 account_ref?: GitHubAccountRef | null;
 /**
  * Opaque reference proving the caller completed its confirmation flow.
  */
 confirmation_evidence_ref: ConfirmationEvidenceRef;
 /**
  * Opaque key that gives one logical action a stable request identity.
  */
 idempotency_key: RepositoryActionIdempotencyKey;
 /**
  * Requested repository mode.
  */
 mode: RepositoryActionCapability;
 /**
  * Exact stable target returned by preview.
  */
 target: RepositoryPreviewTarget;
}

/**
 * Opaque bounded reference to the caller's completed confirmation evidence.
 */
export type ConfirmationEvidenceRef = string;

/**
 * Opaque reference to one connected GitHub account; never a credential.
 */
export type GitHubAccountRef = string;

/**
 * Stable positive GitHub numeric repository identity.
 */
export type GitHubRepositoryNumericId = number;

/**
 * Canonical HTTPS GitHub repository URL with exactly an owner and repository path.
 */
export type GitHubRepositoryUrl = string;

/**
 * Repository actions currently available to the authenticated caller.
 */
export type RepositoryActionCapability = "metadata" | "track" | "star";

/**
 * Bounded opaque identity of one logical repository action.
 */
export type RepositoryActionIdempotencyKey = string;

/**
 * GitHub owner/name alias observed for the repository revision.
 */
export type RepositoryFullName = string;

/**
 * Stable provider identity and current display alias returned by a preview.
 */
export interface RepositoryPreviewTarget {
 /**
  * Canonical HTTPS GitHub repository URL.
  */
 canonical_url: GitHubRepositoryUrl;
 /**
  * Stable positive GitHub numeric repository identity.
  */
 github_repository_numeric_id: GitHubRepositoryNumericId;
 /**
  * Current GitHub owner/name alias.
  */
 repository_full_name: RepositoryFullName;
}
