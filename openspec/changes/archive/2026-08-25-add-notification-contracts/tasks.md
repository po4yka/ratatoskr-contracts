## 1. Scaffolding (configuration - cannot start from a failing test)

- [x] 1.1 Create `crates/notification-contracts` skeleton: package `ratatoskr-notification-contracts` inheriting workspace fields/lints, deps (`ratatoskr-identifiers`, `ratatoskr-event-envelope`, serde, serde_json, schemars, thiserror), lib.rs with crate docs; stub public types (`NotificationRaised`, `NotificationClass`, `NotificationPriority`, `QuietHoursHint`, `NotificationContractError`) with full field shapes, standard derive set and flattened `Extensions`; permissive placeholder bodies where checks will land so later tests fail at runtime rather than compile time. Add root-Cargo.toml member + workspace dependency. Verify: `cargo check -p ratatoskr-notification-contracts` exits 0.
- [x] 1.2 Add `NotificationId` to `crates/identifiers/src/uuid_ids.rs` (kind `notification`) and export it from the identifiers lib. Configuration task following an existing macro: no new behaviour to test beyond the macro's own guarantees. Verify: `cargo check -p ratatoskr-identifiers` exits 0.
- [x] 1.3 Wire discovery: add the dependency to `tools/contractsc/Cargo.toml`; add one sorted entry to `root_types!` in `tools/contractsc/src/registry.rs` and one insert to `event_payload_types()`; append `ratatoskr-telegram` to `[services].known` and `notification` + `analysis` to `[entity_kinds].known` in `contracts.toml`; add the `[[contract]]` block for `platform.notification_raised` (family `events`, owner `ratatoskr-platform`, producers knowledge/github/vault/x, consumer ratatoskr-telegram) with `[contract.event]`. Also one `canonical()` renderer arm in `tools/contractsc/tests/fixtures.rs` and the root-count pins in `tools/contractsc/tests/determinism.rs` (19 roots -> 20, JSON+TypeScript pairs 38 -> 40) - both are manual enumeration points the generator cannot derive. Configuration task: no behaviour exists yet to test. Verify: `cargo contracts check` reports no metadata-rule or registry-set failures; `cargo test -p ratatoskr-contractsc --test fixtures --test determinism` exits 0.
- [x] 1.4 Write valid fixtures `fixtures/events/platform.notification.raised.v1/valid/` - one file per known class (operation-completed, operation-failed, analysis-ready, backup-outcome, watch-triggered, archive-imported), plus `minimal.json` (required members only), `full.json` (every member including both hints and both correlation references) and `unknown-class.json` (`carrier_pigeon`, preserved). Per-class files answer the round-trip-fixture-per-class acceptance item at the gate layer. Generated files cannot start from a failing test. Verify: `cargo contracts generate` then `cargo contracts check` exits 0 with all three valid fixtures accepted.

## 2. Taxonomy and payload behaviours

- [x] 2.1 RED: add tests in `crates/notification-contracts/tests/taxonomy.rs`: `known_classes_parse_to_named_variants` asserting all six tokens map to named variants with stable wire spellings (fails against a stub whose parser rejects everything), and `unknown_class_token_is_preserved` asserting `"carrier_pigeon"` parses as unrecognized, round trips verbatim through serde, and survives a full payload round trip (fails against a rejecting stub). Verify: `cargo test -p ratatoskr-notification-contracts --test taxonomy` fails exactly those assertions.
- [x] 2.2 GREEN: implement `NotificationClass` on the `EntityKind` pattern (PATTERN, MAX_LEN, KNOWN const, `Other(String)` preservation, parse/from_token/as_str/is_known). Verify: the taxonomy tests pass.
- [x] 2.3 RED: add invalid fixture `fixtures/events/platform.notification.raised.v1/invalid/class-token-uppercase.json` (`rejected_by = ["json_schema", "serde"]`). Verify: `cargo contracts check` fails naming the fixture's undeclared acceptance while the schema still publishes no pattern.
- [x] 2.4 GREEN: give `NotificationClass` its manual `JsonSchema` publishing the token grammar so the schema layer rejects malformed tokens too. Verify: `cargo contracts check` exits 0.
- [x] 2.5 RED: add invalid fixture `registry-version-zero.json` (`rejected_by = ["serde"]`) plus assertion in `tests/payload.rs::self_contradicting_payloads_are_refused`. Verify: `cargo contracts check` fails because version zero still deserializes.
- [x] 2.6 GREEN: enforce `class_registry_version > 0` in a checked `Deserialize` following the crate-family checked-intermediate pattern (private wire mirror + `validate()`), which also becomes the drift guard for future fields. Verify: `cargo contracts check` exits 0.

## 3. Delivery hints

- [x] 3.1 RED: add failing test in `tests/hints.rs`: `degenerate_quiet_hours_window_is_refused` asserting equal start/end offsets deserialize to `Err` (fails against a permissive stub). Verify: `cargo test -p ratatoskr-notification-contracts --test hints` fails exactly that assertion.
- [x] 3.2 GREEN: validate `QuietHoursHint` bounds (0..=86_399 each) and inequality inside its checked conversion; publish `minimum`/`maximum` in its manual `JsonSchema`. Verify: the hint test passes.
- [x] 3.3 Closed-vocabulary pin: add invalid fixture `unknown-priority-hint.json` (`rejected_by = ["json_schema", "serde"]`). Exemption: unknown-variant refusal falls out of the derived closed enum the moment it exists - pins regressively like golden output. Verify: `cargo contracts check` exits 0 with the row enforced.
- [x] 3.4 Out-of-range pin: add invalid fixture `quiet-hours-offset-out-of-range.json` (`rejected_by = ["json_schema", "serde"]`) proving the published numeric bound holds at both layers. Exemption: representation-inherent once 3.2 lands. Verify: `cargo contracts check` exits 0.

## 4. Envelope alignment

- [x] 4.1 Create `crates/notification-contracts/tests/envelope.rs` with `event_type_constants_are_the_registered_names` (parses, segments, past-tense action), `notification_travels_inside_a_real_envelope` (`set_payload` binds type, tenant mirrors recipient, canonical round trip, empty `dropped_field_pointers`, typed read back equal), `mismatched_payload_read_is_refused`, and `constructed_payload_authors_nothing_through_extensions` (ADR-0008: empty extensions serialize no extra keys). Exemption: these exercise envelope mechanics that already hold once the payload type and `EventPayload` impl exist - regression pins, not red-green pairs. Verify: `cargo test -p ratatoskr-notification-contracts --test envelope` exits 0.
- [x] 4.2 Per-class round-trip pins: extend `tests/payload.rs` with `every_class_round_trips_losslessly` iterating six committed-style payloads (one per class) through canonical JSON asserting equality and no dropped pointers. Exemption: serialization invariants already guaranteed by derive; this pins them per class as the request's acceptance demands. Verify: the test passes.

## 5. Generated artifacts, baselines, compatibility evidence

- [x] 5.1 Regenerate deterministically and freeze the API surface: run `cargo contracts generate`, then `cargo contracts api-write` to bless `compat/api/ratatoskr-notification-contracts.txt` and re-bless `compat/api/ratatoskr-identifiers.txt` (additive `NotificationId`), reviewing both diffs. Generated/baseline files cannot start from a failing test. Verify: `cargo contracts check` exits 0; `cargo contracts api-check` exits 0.
- [x] 5.2 Record classification evidence: additive by construction (new family member, no prior schema); capture `cargo contracts compat` output between the generated event schema and itself as the identity baseline; note producers/consumers in the change notes. Verification task over tooling output.

## 6. Documentation

- [x] 6.1 Update README contract tree (`notification-contracts (now)`), status paragraph, a "Notifications" principal-models section, and the event-name examples list. Documentation task: no runtime behaviour. Verify: rendered tree lists nine crates.
- [x] 6.2 Update DEVELOPMENT.md present-list, AGENTS.md crate enumeration ("nine contract crates"), docs/ARCHITECTURE.md structure tree plus a notifications subsection beside the other families, and docs/TESTING.md crate count. Verify: grep finds no remaining "eight contract crates" claims outside archived history.

## 7. Gate

- [x] 7.1 Run the full gate block from DEVELOPMENT.md in order (`cargo fetch --locked`, `cargo deny check`, fmt check, clippy `-D warnings`, 850-line size check, `cargo contracts check`, `cargo test --workspace --locked`). Single verification task spanning all implementation groups. Verify: every command exits 0.
